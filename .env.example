# ===========================================
# Environment Configuration
# ===========================================
# Node environment: development, production, or test
# This is usually set automatically by your framework/hosting provider
NODE_ENV=development

# ===========================================
# GraphQL API Configuration
# ===========================================
# URL for the GraphQL API endpoint
# For development with separate backend server: http://localhost:4000/graphql
# For integrated Next.js API route (recommended for Vercel): /api/graphql
NEXT_PUBLIC_GRAPHQL_URL=/api/graphql

# ===========================================
# Backend Server Configuration
# ===========================================
# Port for the standalone backend server (only used when running separate backend)
PORT=4000

# ===========================================
# OpenMeteo API Configuration
# ===========================================
# Note: These are client-side environment variables (NEXT_PUBLIC_)
# They will be embedded in the browser bundle and visible to users
# Geocoding API endpoint for city search
NEXT_PUBLIC_GEOCODING_API=your_geocoding_api_url_here

# Weather forecast API endpoint
NEXT_PUBLIC_WEATHER_API=your_weather_api_url_here
